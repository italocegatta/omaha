## ADDED Requirements

No spec-level requirement changes. Existing spec already mandates percentage 0-100 for `current_deviation_pct` and `projected_deviation_pct`. This change aligns engine code to existing spec contract.

Motivation for delta: document that code-to-spec alignment was deliberate, not a spec change.
