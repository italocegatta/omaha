## 1. Capture baseline and reproduce

- [x] 1.1 **Target:** `pyproject.toml` task entries; `tests/bdd/conftest.py::live_url`; `tests/support/server.py::run_test_server`; `tests/support/browser.py` lifecycle helpers. **Change:** record current port 8766, DB path, child startup/teardown, browser scope, and task boundaries without editing behavior. **Preserve:** BDD serial mode, ports 8765/8766/8767/8768, lane-owned DBs, T32 policy. **Acceptance:** dossier names exact current symbols and no source behavior changed. **Test file/scenario:** `tests/scripts/test_t29_harness.py` existing receipt/runner contracts. **Focused command:** `uv run task test-one tests/scripts/test_t29_harness.py`. **Independent oracle:** capture the path-scoped worktree diff before and after this task; the post-task delta is empty, even if pre-existing T32/worktree changes exist.
- [x] 1.2 **Target:** BDD lane entrypoint `pyproject.toml::test-bdd`. **Change:** capture fresh isolated baseline receipt and first failing node/error if red; do not retry or mask failures. **Preserve:** all 51 collected BDD scenarios and existing workflow assertions. **Acceptance:** receipt records pass/fail count, elapsed time, BDD child/server log path, and port 8766 state. **Test file/scenario:** `tests/bdd/test_scenarios.py` full collected scenario set. **Focused command:** `uv run task test-bdd`. **Independent oracle:** pytest summary plus `tests/bdd/step_defs/_workflows.py::login_and_land` refusal location and `tmp/uvicorn-logs` tail agree on timing.
- [x] 1.3 **Target:** concurrent lane boundary `pyproject.toml::test-bdd` + `test-t32-expanded`; `scripts/run_expanded_lane.py::main`. **Change:** run the two existing taskipy entrypoints concurrently from fresh state and capture first refusal timestamp, BDD child PID/return code, listener/owner of 8766, uvicorn log tail, DB/WAL state, and both receipts. **Preserve:** expanded selection, T32 marker/population, and BDD serial execution. **Acceptance:** reproduction is classified as reproduced or not reproduced with evidence; no source edits and no changed governance selection. **Test file/scenario:** `tests/bdd/test_scenarios.py` scenarios failing at `login_and_land`/other navigation boundaries; T32 expanded selected nodes. **Focused commands:** `uv run task test-bdd` and `uv run task test-t32-expanded` (started concurrently). **Independent oracle:** refusal must correlate with a concrete server/port/process/DB event; otherwise mark hypothesis unconfirmed.

## 2. Diagnose root cause from observed boundary

- [x] 2.1 **Target:** `tests/support/server.py::run_test_server`, `tests/support/browser.py::wait_for_port`, `shutdown_uvicorn`, `tests/bdd/conftest.py::live_url`. **Change:** build a timestamped lifecycle timeline from child creation, readiness, first refusal, child `poll()`/return code, port ownership, log output, and teardown. **Preserve:** no browser retry, no workflow assertion change, no T32 artifact change. **Acceptance:** timeline identifies whether BDD child stayed alive/listening, exited, lost port, or was replaced; each observation has source evidence. **Test file/scenario:** BDD refusal at `/login` and first failing workflow boundary. **Focused command:** `uv run task test-bdd`. **Independent oracle:** process/socket evidence falsifies any claim not matching child and listener state at first refusal.
- [x] 2.2 **Target:** `scripts/run_full_suite.py::{_preflight,_stop,_reap,main}` only if evidence requires it; `scripts/run_expanded_lane.py::main` for ownership comparison. **Change:** compare process groups, signals, return codes, and lane commands against the refusal timeline. **Preserve:** six canonical lanes, pre-run selection, reconciliation, clean-child contract, and 300-second ceiling. **Acceptance:** runner interference is either proven with a signal/process-group event or explicitly falsified; no runner edit is made on correlation alone. **Test file/scenario:** `tests/scripts/test_t29_harness.py` existing process-exit/receipt contracts. **Focused command:** `uv run task test-one tests/scripts/test_t29_harness.py`. **Independent oracle:** child PID/process-group timeline has no unexplained BDD signal or sibling reap event when H2 is falsified.
- [x] 2.3 **Target:** diagnosis record in this change `design.md`. **Change:** select exactly one confirmed hypothesis H1/H2/H3, state symptoms, reproducible conditions, observability, falsification condition, and minimum correction scope; if evidence remains ambiguous, stop. **Preserve:** T32 ownership boundary and no trial-and-error remediation. **Acceptance:** selected hypothesis passes evidence test and alternatives are falsified, or status is `BLOCKED_FOR_IMPLEMENTATION_BRIEF`. **Test file/scenario:** focused reproduction evidence from 1.3. **Focused command:** `uv run task test-bdd`. **Independent oracle:** another reviewer can reproduce classification from retained receipt/log/process artifacts without relying on a code guess.

## 3. Validate hypothesis with focused controlled test

- [x] 3.1 **Target:** `tests/scripts/test_t29_harness.py`; confirmed lifecycle/runner symbol from 2.3. **Change:** add deterministic unit contract that reproduces the confirmed boundary with controlled subprocess/socket/process doubles and asserts the required failure/ownership/reaping behavior before runtime code changes. **Preserve:** existing T29 receipt, reconciliation, selection, duration, and signal tests; keep file unit-owned and do not alter `_INTEGRATION_PREFIXES`/`_UNIT_FILES`. **Acceptance:** controlled test fails against the defect model and passes only when the confirmed invariant is satisfied; no browser retry or masked pass. **Test file/scenario:** new T33-named focused test(s) in `tests/scripts/test_t29_harness.py`. **Focused command:** `uv run task test-one tests/scripts/test_t29_harness.py`. **Independent oracle:** test fails if child exits before readiness, wrong process owns 8766, teardown leaves child/port alive, or proven runner signal contract regresses—whichever condition diagnosis selected.
- [x] 3.2 **Target:** `tests/conftest.py` marker hook and T32 governance inputs. **Change:** verify focused test collection/classification and expanded selection remain unchanged; edit neither allow-list nor governance policy unless a separate, explicit scope decision is authorized. **Preserve:** explicit allow-list, current node IDs, T32 marker, skips, and selection order. **Acceptance:** focused harness test is unit-classified without new prefix changes and T32 expanded candidate set/checksums are identical before/after. **Test file/scenario:** `tests/scripts/test_t29_harness.py` plus existing governance tests. **Focused command:** `uv run task test-one tests/scripts/test_t29_harness.py`. **Independent oracle:** collection markers and `test-t32-expanded` manifest/selection diff are empty.

## 4. Apply only minimal confirmed correction

- [x] 4.1 **Target:** exact symbol/file selected by 2.3 and covered by 3.1. **Change:** implement the smallest lifecycle/ownership/resource correction that makes the controlled test pass; prefer existing `run_test_server`, `wait_for_port`, `shutdown_uvicorn`, and logging primitives. **Preserve:** BDD session server ownership of 8766, unique neighboring ports, lane DB isolation, serial BDD execution, and abnormal-exit visibility. **Acceptance:** changed file/symbol set matches the diagnosis map; no speculative branch, browser retry, blanket serialization, lane removal, skip, xfail, or coverage reduction. **Test file/scenario:** T33 controlled contract in `tests/scripts/test_t29_harness.py`. **Focused command:** `uv run task test-one tests/scripts/test_t29_harness.py`. **Independent oracle:** focused oracle passes and the implementation delta from the recorded T33 baseline contains no T32 artifact, roadmap, production file, scenario file, or unrelated file.
- [x] 4.2 **Target:** `tests/bdd/conftest.py::live_url` and `tests/bdd/step_defs/_workflows.py` as preservation checks. **Change:** update only harness wiring required by 4.1; do not alter `login_and_land`, `create_one_class`, `add_one_asset`, D8 same-URL behavior, response assertions, or rendered-row completion waits. **Preserve:** existing workflow input/output boundaries and all 51 scenarios. **Acceptance:** any BDD fixture change leaves workflow diff empty; if diagnosis points at workflow/navigation instead of server lifecycle, stop for owner scope decision rather than reopening T32 remediation. **Test file/scenario:** exact R3 target `tests/bdd/test_scenarios.py::test_clear_class_target_enter_saves_zero[Italo]`. **Focused command:** `uv run task test-one 'tests/bdd/test_scenarios.py::test_clear_class_target_enter_saves_zero[Italo]'`. **Independent oracle:** response status and class-row visibility assertions remain present and pass.

## 5. Validate isolated, concurrent, and canonical delivery

- [x] 5.1 **Target:** BDD isolated lane and harness contract tests. **Change:** run focused contracts and full isolated BDD after correction; retain receipts and server logs. **Preserve:** 51 scenarios, serial browser lane, no coverage/pruning changes. **Acceptance:** `test_t29_harness.py` passes and `uv run task test-bdd` reports 51 passed, 0 failed. **Test file/scenario:** `tests/scripts/test_t29_harness.py` and all `tests/bdd/test_scenarios.py`. **Focused commands:** `uv run task test-one tests/scripts/test_t29_harness.py`; `uv run task test-bdd`. **Independent oracle:** pytest summaries plus BDD lane receipt show no refusal and clean server teardown.
- [x] 5.2 **Target:** concurrent BDD/expanded lane. **Change:** repeat controlled concurrent task pair after correction. **Preserve:** `test-t32-expanded` selection and T32 receipts, BDD serial mode, port 8766 ownership. **Acceptance:** BDD remains 51 passed/0 failed while expanded lane completes its governed cases; no `ERR_CONNECTION_REFUSED`, stale listener, or orphan child. **Test file/scenario:** full BDD scenarios and versioned T32 expanded selections. **Focused commands:** `uv run task test-bdd` and `uv run task test-t32-expanded` (started concurrently). **Independent oracle:** both task receipts, port probe, child cleanup, and uvicorn log tails agree.
- [x] 5.3 **Target:** canonical full-suite runner `scripts/run_full_suite.py::main`. **Change:** run final gate without altering lane scheduling or governance policy. **Preserve:** all six lanes, current population/checksums, two exact skips, clean children, and <=300 seconds. **Acceptance:** `uv run task test` exits 0, reports all six lanes green, `clean_children=true`, reconciled current lanes/checksums/skips, and elapsed wall-clock `<=300s`. **Test file/scenario:** unit, integration, audit, e2e, BDD, and visual lane suites. **Focused command:** `uv run task test`. **Independent oracle:** canonical JSON receipt under `reports/test-profile/` plus runner exit code.
- [x] 5.4 **Target:** OpenSpec change/spec health and scope diff. **Change:** validate exact T33 artifacts and all repository specs; do not edit roadmap, T32 artifacts, or unrelated files. **Preserve:** T32 proposal/design/tasks/specs unchanged and no new pruning/coverage policy. **Acceptance:** strict change and strict spec validation pass; the delta from the recorded T33 baseline contains only approved T33 artifacts and diagnosis-proven implementation/tests. **Test file/scenario:** OpenSpec validator plus focused T33 tests. **Focused command:** `uv run openspec validate t33-corrigir-concorrencia-e-ciclo-de-vida-do-harness-bdd --strict && uv run openspec validate --specs --strict`. **Independent oracle:** validator reports zero failures and a scope audit finds no new T32/roadmap/unrelated edits beyond pre-existing worktree state.

## Execution Evidence

### Initial apply preflight

- Baseline boundary: `git diff HEAD~1` captured before T33 edits. Existing T32/worktree changes listed in `design.md` T33-DIAG-01; no attempt made to overwrite them.
- `uv run task test-one tests/scripts/test_t29_harness.py` -> 31 passed in 0.11s before T33 test edits.
- Initial `uv run task test-bdd` reached 45/51 at the tool's 180s limit; no failure was observed before termination. Fresh controlled concurrent run completed BDD 51/51 and expanded 12/12 selected + 8 deselected, both exit 0.
- Controlled timeline: BDD parent PID 344984; BDD uvicorn PID 345020; port 8766 was unavailable only during startup, then accepting through completion; expanded parent PID 344985 exited 0 and did not own 8766. No refusal, signal, child exit, or uvicorn error observed.
- Scope check before implementation: `tests/conftest.py`, `tests/bdd/step_defs/_workflows.py`, `scripts/run_full_suite.py`, `scripts/run_expanded_lane.py`, T32 artifacts, and application code unchanged by T33.

### Implementation and focused validation

- RED: after adding `test_t33_server_does_not_accept_stale_listener_for_dead_child`, `uv run task test-one tests/scripts/test_t29_harness.py` -> 1 failed, 31 passed in 0.27s; fake dead child plus controlled stale listener yielded URL under old helper.
- GREEN: same command after fix -> 32 passed in 0.15s. Changed symbols: `tests/support/browser.py::wait_for_port` checks child `poll()`; `tests/support/server.py::run_test_server` passes child and uses bounded `shutdown_uvicorn` on startup failure; `tests/scripts/test_t29_harness.py` adds controlled stale-listener contract.
- `uv run task test-one 'tests/bdd/test_scenarios.py::test_clear_class_target_enter_saves_zero[Italo]'` -> 1 passed in 7.21s.
- `uv run task test-bdd` -> 51 passed in 195.43s; no refusal; session server teardown completed.
- Post-fix controlled concurrent task pair -> BDD exit 0, expanded exit 0; BDD 51 passed, expanded 12 passed + 8 deselected. Port timeline: 8766 startup-unavailable, accepting at 6.17s, released at 205.16s after BDD completion. No refusal, stale listener, orphan child, or workflow change.
- Governance preservation: T33 test stayed in existing `pytestmark = pytest.mark.unit`; no changes to `_INTEGRATION_PREFIXES`, `_UNIT_FILES`, T32 selection, marker, skip, checksum, or population inputs.
- Deferred gate: task 5.3 (`uv run task test`) remains open because full-suite execution belongs exclusively to `review` per apply gate contract.
- `uv run ruff check tests/support/browser.py tests/support/server.py tests/scripts/test_t29_harness.py` -> all checks passed; `git diff --check` -> clean.
- `uv run openspec validate t33-corrigir-concorrencia-e-ciclo-de-vida-do-harness-bdd --strict && uv run openspec validate --specs --strict` -> 68 specs passed, 0 failed.
- Final focused rerun: `uv run task test-one tests/scripts/test_t29_harness.py` -> 32 passed in 0.20s; no related test remains broken.
- Clean T33 diff boundary: new lifecycle changes are limited to `tests/support/browser.py::wait_for_port`, `tests/support/server.py::run_test_server`, and the T33 contract in `tests/scripts/test_t29_harness.py`; pre-existing T32 hunks in `pyproject.toml`, `scripts/run_full_suite.py`, `tests/conftest.py`, `_workflows.py`, and T32 files remain untouched.

### Aggregate commit pre-hook remediation

- Preflight confirmed aggregate work staged across the existing T32/T33 and repository documentation paths. Six unstaged hook edits were retained because they are formatter/Ruff autofix-only, behavior-neutral changes within already-staged aggregate paths: `scripts/run_expanded_lane.py`, `scripts/test_governance.py`, `tests/bdd/step_defs/common_steps.py`, `tests/conftest.py`, `tests/e2e/conftest.py`, and `tests/support/browser.py`. No unrelated or semantic hunk was introduced.
- Changed symbol: `tests/conftest.py::pytest_collection_modifyitems`; reformatted only the long integration-prefix condition reported as E501 at line 283. No marker or governance behavior changed.
- Validation: `uv run task lint` -> passed; `uv run ruff check .` -> passed; `git diff --check` -> passed. `task lint`'s existing `pytest-unit` hook also passed; no targeted tests were rerun for this formatting-only correction.
- No roadmap, commit, push, archive, runner, or behavioral test changes made.

## Review Findings

### Review R1
Scope audit: requirements/scenarios pass; task/design/proposal coherence pass; H1 diagnosis and RED contract pass; child-aware readiness and bounded startup teardown pass; isolated BDD pass; concurrent BDD + expanded lane pass; harness focused suite pass; strict change/spec validation pass; T32 pruning/governance/population/marker/skip scope pass; application behavior and production scope pass; changed-symbol boundary pass; full-suite duration/cleanup contract pass. No area not assessable.
Full suite: `uv run task test` -> green, exit 0, 265.92s wall-clock (runner receipt 265.6637s), cleanup `clean_children=true`; duration limit 300s; all six lanes exit 0, 479 unit passed + 2 exact skips, reconciliation `ok=true`, expected skips equal actual skips. Test gate passed.
Focused checks: `uv run task test-one tests/scripts/test_t29_harness.py` -> 32 passed in 0.19s. `uv run task test-bdd` -> 51 passed in 186.34s. Concurrent `uv run task test-bdd` + `uv run task test-t32-expanded` -> both exit 0; BDD 51 passed in 206.58s; expanded 12 passed, 8 deselected in 130.36s. `uv run openspec validate t33-corrigir-concorrencia-e-ciclo-de-vida-do-harness-bdd --strict && uv run openspec validate --specs --strict` -> 68 passed, 0 failed.
Verdict: APPROVED

No blocking findings. H1 correction is minimal and durable: `wait_for_port` rejects a dead child before accepting stale TCP readiness (`tests/support/browser.py:31-57`), `run_test_server` passes child state and uses bounded diagnostic teardown on startup failure (`tests/support/server.py:66-79`), and T33 contract reproduces dead-child/stale-listener RED behavior (`tests/scripts/test_t29_harness.py:20-59`). H2/H3 remain falsified by dossier evidence; no runner, workflow, application, T32 governance, pruning, or coverage changes found.
