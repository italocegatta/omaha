# Specs — T10 CSV Pipeline Audit

No new or modified capabilities. This is an audit-only slice:
- `csv-seed-internals` — verified against implementation (no requirement changes)
- `data-driven-seed` — verified against implementation (no requirement changes)
- `seeded-state` — verified against implementation (no requirement changes)

If contract drift is found during audit, a delta spec file will be created
in the appropriate subdirectory (e.g., `specs/data-driven-seed/spec.md`).
