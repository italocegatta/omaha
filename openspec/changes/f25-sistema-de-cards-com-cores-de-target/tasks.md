## 1. Template contract

- [ ] 1.1 Update `_rebalance_plan.html` card header so class name is primary text and kicker label `CLASSE` is removed.
- [ ] 1.2 Keep class summary card markup aligned to one shared family structure across all states.

## 2. Card styling

- [ ] 2.1 Refactor rebalance class-card CSS into one base shell with state modifiers for above-target and below-target accents.
- [ ] 2.2 Tune border, tint, and metric emphasis so cards read as one family instead of separate molds.

## 3. Verification

- [ ] 3.1 Inspect rendered rebalance page to confirm positive/negative target accents and unchanged empty-state behavior.
- [ ] 3.2 Run spec-health / targeted UI checks for rebalance page before handing off to apply.
