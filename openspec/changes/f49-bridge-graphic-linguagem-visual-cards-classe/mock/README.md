# F49 phase-1 static mock

Open `index.html` directly or serve this directory with:

```text
python -m http.server 8765 --directory openspec/changes/f49-bridge-graphic-linguagem-visual-cards-classe/mock
```

Preview: `http://127.0.0.1:8765/index.html`

Fixtures are literal, static approval data. No runtime field mapping is inferred.

## Local scaling rule

Each card has private BRL axis and strict next-tick ceiling: Ações `R$0–R$200k`
with five ticks; FII `R$0–R$150k` with four ticks. Total columns start at R$0.
Operation and residual marks float between cumulative levels. Percentages are
literal text companions and never feed width, position, or comparison.
Cards do not share scale.

The unresolved runtime percentage mapping remains intentionally outside this
mock: operation and residual fixture percentages are literal display values.
