---
phase: 01-audit
plan: 01-02
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/omaha/audit/__init__.py
  - src/omaha/audit/css_parser.py
  - src/omaha/audit/color_resolver.py
  - tests/test_audit_css_parser.py
  - tests/test_audit_color_resolver.py
autonomous: false
requirements:
  - AUDT-02
must_haves:
  truths:
    - Audit lists every CSS custom property that resolves to a color and its adjacent-background contrast.
    - Each token row shows ratio, AA status, and failure flag.
    - CSS parser resolves var(), color-mix(), and filter brightness declarations.
  artifacts:
    - path: src/omaha/audit/css_parser.py
      provides: CSS parsing and token inventory
    - path: src/omaha/audit/color_resolver.py
      provides: WCAG 2.1 contrast computation
    - path: tests/test_audit_css_parser.py
      provides: AUDT-02 token extraction tests
    - path: tests/test_audit_color_resolver.py
      provides: AUDT-02 contrast tests
  key_links:
    - from: css_parser.color_token_inventory
      to: color_resolver.aa_status
      via: function call
    - from: css_parser.parse_stylesheet
      to: tinycss2.parse_stylesheet
      via: library call
---

<objective>
Build the foundational CSS parser and color resolver for the Phase 1 contrast audit.

Purpose: AUDT-02 needs a trustworthy inventory of every CSS color token and its computed contrast before palette fixes are applied.
Output: `src/omaha/audit/css_parser.py`, `src/omaha/audit/color_resolver.py`, and their tests.
</objective>

<execution_context>
@/home/italo/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/italo/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-audit/01-RESEARCH.md
@.planning/phases/01-audit/01-PATTERNS.md
@.planning/phases/01-audit/01-UI-SPEC.md
@src/omaha/static/app.css
@src/omaha/routes/__init__.py
@pyproject.toml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify package legitimacy for audit dependencies</name>
  <files>pyproject.toml</files>
  <read_first>
    - /home/italo/github/omaha/.planning/phases/01-audit/01-RESEARCH.md
    - /home/italo/github/omaha/pyproject.toml
  </read_first>
  <what-built>Nothing yet; this gate blocks installation of the four [SUS] packages.</what-built>
  <how-to-verify>
    1. Open each official PyPI page:
       - https://pypi.org/project/beautifulsoup4/
       - https://pypi.org/project/tinycss2/
       - https://pypi.org/project/coloraide/
       - https://pypi.org/project/lxml/
    2. Confirm the current versions satisfy the minimums in 01-RESEARCH.md:
       beautifulsoup4>=4.15.0, tinycss2>=1.5.1, coloraide>=8.8.1, lxml>=6.1.1.
    3. Cross-check the package source URLs from the RESEARCH.md Package Legitimacy Audit table.
  </how-to-verify>
  <resume-signal>Type "approved" to allow installation, or "reject" to abort the phase.</resume-signal>
  <action>
    Pause before adding beautifulsoup4, tinycss2, coloraide, and lxml to [dependency-groups] dev. On approval, execution continues to Task 2; on rejection, the plan stops and no packages are installed.
  </action>
  <verify>
    <human-check>Approve only after confirming the four packages match their official PyPI pages and the required minimum versions.</human-check>
  </verify>
  <acceptance_criteria>
    - User explicitly approves the package legitimacy checkpoint.
  </acceptance_criteria>
  <done>All four [SUS] packages approved for installation, or the phase is aborted.</done>
</task>

<task type="auto">
  <name>Task 2: Add dev dependencies and create audit package stubs</name>
  <files>pyproject.toml, src/omaha/audit/__init__.py, src/omaha/audit/css_parser.py, src/omaha/audit/color_resolver.py, tests/test_audit_css_parser.py, tests/test_audit_color_resolver.py</files>
  <read_first>
    - /home/italo/github/omaha/pyproject.toml
    - /home/italo/github/omaha/.planning/phases/01-audit/01-RESEARCH.md
    - /home/italo/github/omaha/.planning/phases/01-audit/01-PATTERNS.md
    - /home/italo/github/omaha/tests/test_t02_csv_import.py
    - /home/italo/github/omaha/src/omaha/csv_import.py
    - /home/italo/github/omaha/src/omaha/routes/__init__.py
  </read_first>
  <action>
    Run `uv add --dev coloraide>=8.8.1 tinycss2>=1.5.1 beautifulsoup4>=4.15.0 lxml>=6.1.1`. Create `src/omaha/audit/__init__.py` mirroring `src/omaha/routes/__init__.py` (module docstring, `from __future__ import annotations`, `__all__: list[str] = []`). Create `src/omaha/audit/css_parser.py` and `src/omaha/audit/color_resolver.py` following the pure-function/dataclass module shape in `src/omaha/csv_import.py`, with module docstrings, `from __future__ import annotations`, and minimal placeholder functions (`parse_stylesheet`, `color_token_inventory`, `contrast_ratio`, `aa_status`) so the modules are importable. Create `tests/test_audit_css_parser.py` and `tests/test_audit_color_resolver.py` following the analog `tests/test_t02_csv_import.py` from `01-PATTERNS.md`, starting with import/signature tests only. Do not implement resolution logic yet.
  </action>
  <verify>
    <automated>uv run pytest tests/test_audit_css_parser.py tests/test_audit_color_resolver.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` `[dependency-groups] dev` contains `coloraide>=8.8.1`, `tinycss2>=1.5.1`, `beautifulsoup4>=4.15.0`, and `lxml>=6.1.1`.
    - `src/omaha/audit/__init__.py`, `css_parser.py`, and `color_resolver.py` exist and are importable via `from omaha.audit import css_parser, color_resolver`.
    - Both test files exist and pass (only import/signature assertions).
  </acceptance_criteria>
  <done>Dev dependencies installed and audit package stubs are importable.</done>
</task>

<task type="auto">
  <name>Task 3: Implement CSS parser, color resolver, and token inventory</name>
  <files>src/omaha/audit/css_parser.py, src/omaha/audit/color_resolver.py, tests/test_audit_css_parser.py, tests/test_audit_color_resolver.py</files>
  <read_first>
    - /home/italo/github/omaha/src/omaha/static/app.css
    - /home/italo/github/omaha/.planning/phases/01-audit/01-RESEARCH.md
    - /home/italo/github/omaha/.planning/phases/01-audit/01-UI-SPEC.md
    - /home/italo/github/omaha/.planning/phases/01-audit/01-PATTERNS.md
    - /home/italo/github/omaha/tests/test_t02_csv_import.py
    - /home/italo/github/omaha/src/omaha/csv_import.py
  </read_first>
  <action>
    In `css_parser.py` implement `parse_stylesheet(path)` using `tinycss2.parse_stylesheet(css_text, skip_comments=True, skip_whitespace=True)`; extract qualified-rule selectors and declarations; build a custom-property registry from `:root` and component-scoped rules; implement `resolve_var(value, registry)` to substitute `var()` tokens recursively; expose `color_token_inventory(stylesheet)` returning dataclass rows for every `--*` custom property whose resolved value parses as a color, pairing each token with a default adjacent background (`--bg` for foreground-ish tokens `--ink`, `--ink-muted`, `--fg`, `--muted`, `--accent-ink`, `--positive`, `--negative`, `--error-fg`, `--class-1`..`--class-6`; `--surface` for surface-ish tokens `--bg`, `--surface`, `--surface-sunk`, `--error-bg`, `--accent`). In `color_resolver.py` implement `contrast_ratio(fg, bg)` using `Color(fg).contrast(bg, method="wcag21")`, `aa_status(ratio, is_large=False)` returning `(ratio, "Passa"/"Falha")` with thresholds `4.5` and `3.0`, `apply_brightness(color, factor)` scaling sRGB channels, and `composite_over(color, backdrop)` for alpha blending. Model output dataclasses and function contracts after `src/omaha/csv_import.py` (e.g., `MatchResult`). Add tests that assert token extraction from a fixture CSS, contrast values for known pairs, AA failure flagging below 4.5, and `color-mix()`/brightness handling.
  </action>
  <verify>
    <automated>uv run pytest tests/test_audit_css_parser.py tests/test_audit_color_resolver.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `color_token_inventory(parse_stylesheet(app_css_path))` returns at least the tokens defined in `:root` (e.g., `--bg`, `--surface`, `--ink`, `--accent`).
    - Each token row has fields: `token` (str), `computed_value` (str), `adjacent_background` (str), `ratio` (float), `status` ("Passa" or "Falha").
    - `contrast_ratio("oklch(0.975 0.003 60)", "oklch(0.20 0.01 60)")` returns a value >= 4.5.
    - `aa_status(4.4, False)` returns status `"Falha"`; `aa_status(3.1, True)` returns status `"Passa"`.
    - `apply_brightness("#0a66c2", 1.1)` returns a lighter hex string.
    - All tests pass.
  </acceptance_criteria>
  <done>CSS parser and color resolver produce token inventory rows with WCAG AA status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> audit script | Parsed CSS/JS never executes; only project files under repo root are read. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | `css_parser.parse_stylesheet` file open | mitigate | Resolve `path` under repo root with `Path(__file__).resolve().parents[3]`; reject paths containing `..` after normalization. |
| T-01-02-02 | Tampering | `tinycss2` / `coloraide` package install | mitigate | Blocking human checkpoint in Task 1; packages verified on official PyPI pages before install. |
| T-01-02-03 | Tampering | Untrusted CSS/JS execution | mitigate | Use `tinycss2` and `coloraide` parsers that do not execute code; never `eval()` parsed values. |
</threat_model>

<verification>
Run the AUDT-02 focused test suite:

```bash
uv run pytest tests/test_audit_css_parser.py tests/test_audit_color_resolver.py -q
```

Then confirm `color_token_inventory` runs against the real `app.css` and returns rows for every `:root` color token.
</verification>

<success_criteria>
- `css_parser.py` and `color_resolver.py` are fully implemented and tested.
- `color_token_inventory` lists every `:root`/component-scoped custom property that holds a color, with resolved value, adjacent background, contrast ratio, and Passa/Falha status.
- All AUDT-02 tests pass and the package legitimacy checkpoint is recorded.
</success_criteria>

<artifacts_this_phase_produces>
## New files
- `src/omaha/audit/__init__.py`
- `src/omaha/audit/css_parser.py`
- `src/omaha/audit/color_resolver.py`
- `tests/test_audit_css_parser.py`
- `tests/test_audit_color_resolver.py`

## New symbols in `css_parser.py`
- dataclass `CssRule`
- dataclass `CssToken`
- dataclass `TokenInventoryRow`
- function `parse_stylesheet(path: Path) -> Stylesheet`
- function `resolve_var(value: str, registry: dict[str, str]) -> str`
- function `color_token_inventory(stylesheet: Stylesheet) -> list[TokenInventoryRow]`

## New symbols in `color_resolver.py`
- dataclass `ContrastResult`
- function `contrast_ratio(fg: str, bg: str) -> float`
- function `aa_status(ratio: float, is_large: bool = False) -> tuple[float, str]`
- function `apply_brightness(color: str, factor: float) -> str`
- function `composite_over(color: str, backdrop: str) -> str`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-audit/01-02-SUMMARY.md` when done.
</output>
