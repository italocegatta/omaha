---
phase: 02-palette
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/omaha/static/app.css
autonomous: true
requirements:
  - PALT-01
  - PALT-02
must_haves:
  truths:
    - "app.css :root block defines --class-4 with oklch(0.53 0.13 50) ratio ≥ 4.5:1 vs --bg"
    - "app.css :root block defines --class-6 with oklch(0.52 0.10 200) ratio ≥ 4.5:1 vs --bg"
    - "app.css :root block defines --negative-ink and --positive-ink tokens"
    - "app.css :root block defines --error-bg and --error-fg in OKLCH format"
    - "Delete-confirm buttons use var(--negative-ink) instead of hardcoded #fff"
    - "Legacy aliases --fg → var(--ink) and --muted → var(--ink-muted) remain intact"
    - "color_token_inventory() returns zero Falha rows after all changes"
  artifacts:
    - path: "src/omaha/static/app.css"
      provides: "Corrected color token system in :root block"
      contains: "oklch(0.53 0.13 50)"
      contains: "oklch(0.52 0.10 200)"
      contains: "--negative-ink"
      contains: "--positive-ink"
      contains: "var(--negative-ink)"
  key_links:
    - from: ".class-delete-confirm-yes"
      to: "--negative-ink"
      via: "color: var(--negative-ink)"
      pattern: "color:\\s*var\\(--negative-ink\\)"
    - from: ".dashboard-asset-delete-confirm-yes"
      to: "--negative-ink"
      via: "color: var(--negative-ink)"
      pattern: "color:\\s*var\\(--negative-ink\\)"
---

<objective>
Correct `app.css` :root block tokens: fix 2 class color failures (`--class-4`, `--class-6`), add 2 status-ink tokens (`--negative-ink`, `--positive-ink`), convert 2 error tokens to OKLCH, replace hardcoded `#fff` with `var(--negative-ink)` in 2 delete-confirm button rules.

Purpose: Satisfies PALT-01 (unambiguous fg/bg pairs for every surface) by formalizing the implicit pairs currently using hardcoded hex values. Satisfies PALT-02 (documented contrast ratios) by ensuring every token passes WCAG AA threshold.

Output: Corrected `app.css` :root block (lines 3–58 updated) with all tokens verified via `color_token_inventory()`. Two component rules (lines 1085, 1186) updated to reference the new `--negative-ink` token.
</objective>

<execution_context>
@/home/italo/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/italo/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-palette/02-CONTEXT.md
@.planning/phases/02-palette/02-RESEARCH.md
@.planning/phases/02-palette/02-PATTERNS.md
@src/omaha/static/app.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Correct :root block color tokens — fix failures, add tokens, convert to OKLCH</name>
  <files>src/omaha/static/app.css</files>
  <read_first>
src/omaha/static/app.css (lines 1–58 — current :root block)
.planning/phases/02-palette/02-RESEARCH.md (lines 492–551 — corrected :root block reference)
.planning/phases/02-palette/02-PATTERNS.md (lines 80–165 — app.css edit patterns per section)
  </read_first>
  <behavior>
    - Behavior 1: After Task 1, `color_token_inventory()` run against the real `app.css` returns zero rows with `status == 'Falha'`.
    - Behavior 2: After Task 1, `--class-4` value is `oklch(0.53 0.13 50)` — not `#ef6c00`.
    - Behavior 3: After Task 1, `--class-6` value is `oklch(0.52 0.10 200)` — not `#00838f`.
    - Behavior 4: After Task 1, `--negative-ink` token exists with value `#ffffff`.
    - Behavior 5: After Task 1, `--positive-ink` token exists with value `#ffffff`.
    - Behavior 6: After Task 1, `--error-bg` value is `oklch(0.94 0.02 15)` — not `#fde8e8`.
    - Behavior 7: After Task 1, `--error-fg` value is `oklch(0.38 0.12 25)` — not `#8a1f1f`.
    - Behavior 8: After Task 1, legacy aliases `--fg: var(--ink)` and `--muted: var(--ink-muted)` still exist verbatim in :root block.
  </behavior>
  <action>
Modify the :root block (lines 3–58 of app.css) with these exact changes. Work by editing the existing file — do NOT replace the entire :root block or the entire file.

**1. Fix class color failures (lines 50, 52):**
Replace `--class-4: #ef6c00; /* orange */` with:
```
  --class-4: oklch(0.53 0.13 50);            /* orange — 5.16, Passa (was #ef6c00 at 2.87 Falha) */
```
Replace `--class-6: #00838f; /* teal   */` with:
```
  --class-6: oklch(0.52 0.10 200);           /* teal   — 4.89, Passa (was #00838f at 4.21 Falha) */
```

**2. Add contrast comments to passing class tokens (lines 47–49, 51):**
Update inline comments for class tokens that stay unchanged:
- `--class-1: #0a66c2;` → append `/* blue   — 5.29, Passa */`
- `--class-2: #2e7d32;` → append `/* green  — 4.77, Passa */`
- `--class-3: #c62828;` → append `/* red    — 5.23, Passa */`
- `--class-5: #6a1b9a;` → append `/* purple — 8.73, Passa */`

**3. Update class color section comment (lines 40–46):**
Replace the block comment above class colors (lines 40–46) with:
```css
  /* Class color tokens — WCAG AA verified against --bg.
     Class-4 and class-6 corrected from failing hex to passing OKLCH.
     Classes 1,2,3,5 already pass at current values. */
```

**4. Convert error tokens to OKLCH (lines 36–38):**
Replace the error feedback block:
```css
  /* App-specific error feedback — not in the design token table. */
  --error-bg: #fde8e8;
  --error-fg: #8a1f1f;
```
with:
```css
  /* Error feedback — converted to OKLCH for consistency.
     Contrast: --error-fg on --error-bg = 7.79:1 Passa. */
  --error-bg: oklch(0.94 0.02 15);
  --error-fg: oklch(0.38 0.12 25);
```

**5. Add status ink tokens (after line 27, before legacy aliases line 29):**
After `--negative: oklch(0.50 0.18 25);` (line 27), insert:
```css

  /* Status ink tokens — text on status backgrounds.
     --negative-ink: text on --negative fill (6.59:1, Passa).
     --positive-ink: text on --positive fill (future). */
  --negative-ink: #ffffff;
  --positive-ink: #ffffff;
```

**6. Add contrast note to accent token comment (line 21–23):**
Update the accent block comment header to include the contrast ratio:
Change:
```css
  /* Accent — single committed color (deep fern, hue 150). */
```
to:
```css
  /* Accent — single committed color (deep fern, hue 150).
     Contrast: --accent-ink on --accent = 7.67:1 Passa. */
```

**Verification before considering this task done:**
Run the token inventory and confirm zero failures:
```bash
uv run python3 -c "
from omaha.audit.css_parser import parse_stylesheet, color_token_inventory
from pathlib import Path
sheet = parse_stylesheet(Path('src/omaha/static/app.css'))
rows = color_token_inventory(sheet)
failures = [r for r in rows if r.status == 'Falha']
if failures:
    print('FAILURES:', [r.token for r in failures])
    exit(1)
else:
    print('All tokens pass WCAG AA ✓')
"
```

Do NOT change any other token values. Do NOT delete legacy aliases. Do NOT change surface tokens, ink tokens, border tokens, accent values, positive/negative values, or `--color-focus`.
  </action>
  <verify>
    <automated>uv run python3 -c "from omaha.audit.css_parser import parse_stylesheet, color_token_inventory; from pathlib import Path; sheet = parse_stylesheet(Path('src/omaha/static/app.css')); rows = color_token_inventory(sheet); failures = [r for r in rows if r.status == 'Falha']; assert len(failures) == 0, [r.token for r in failures]; print(f'All {len(rows)} tokens pass WCAG AA ✓')"</automated>
  </verify>
  <done>
- token_inventory() returns zero Falha rows (was 2 failures: class-4 at 2.87, class-6 at 4.21)
- --class-4 value is oklch(0.53 0.13 50) with inline comment showing ratio 5.16 Passa
- --class-6 value is oklch(0.52 0.10 200) with inline comment showing ratio 4.89 Passa
- --negative-ink and --positive-ink tokens exist with value #ffffff
- --error-bg and --error-fg use OKLCH format (not hex)
- Legacy aliases --fg and --muted remain unchanged
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace hardcoded color: #fff with var(--negative-ink) in delete-confirm buttons</name>
  <files>src/omaha/static/app.css</files>
  <read_first>
src/omaha/static/app.css (lines 1081–1090 — .class-delete-confirm-yes rule)
src/omaha/static/app.css (lines 1182–1191 — .dashboard-asset-delete-confirm-yes rule)
  </read_first>
  <behavior>
    - Behavior 1: After Task 2, `grep -c 'color:\s*#fff' src/omaha/static/app.css` returns 0 (no hardcoded white in component rules).
    - Behavior 2: After Task 2, `grep -c 'color:\s*var(--negative-ink)' src/omaha/static/app.css` returns 2 (both delete-confirm buttons use the token).
  </behavior>
  <action>
**Location 1 — .class-delete-confirm-yes (line 1085):**
Replace `color: #fff;` with `color: var(--negative-ink);` inside the `.class-delete-confirm-yes` rule (line ~1085).

**Location 2 — .dashboard-asset-delete-confirm-yes (line 1186):**
Replace `color: #fff;` with `color: var(--negative-ink);` inside the `.dashboard-asset-delete-confirm-yes` rule (line ~1186).

Both rules use `background: var(--negative)` — the formalized pair is now `--negative` (background) + `--negative-ink` (foreground). This satisfies PALT-01's "unambiguous fg/bg pairs for every surface" requirement at the component level.

Do NOT change anything else in these rules (hover states, disabled opacity, border, padding, etc.). Do NOT touch any other `color: #fff` that may exist in :root declarations or non-component contexts.
  </action>
  <verify>
    <automated>bash -c 'test $(grep -c "color: var(--negative-ink)" src/omaha/static/app.css) -eq 2 && test $(grep -c "^  color: #fff;" src/omaha/static/app.css) -eq 0 && echo "Both delete-confirm buttons use var(--negative-ink) ✓"'</automated>
  </verify>
  <done>
- .class-delete-confirm-yes uses color: var(--negative-ink) instead of color: #fff
- .dashboard-asset-delete-confirm-yes uses color: var(--negative-ink) instead of color: #fff
- No other component rules have hardcoded color: #fff (surface tokens in :root are exempt from this check)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → app.css | CSS file parseable by tinycss2; no user-controllable CSS input |
| Browser rendering → Human | Visual rendering of corrected tokens verified via DevTools contrast checker (Phase 4) |

## Assets at Risk

| Asset | Value | Exposure |
|-------|-------|----------|
| app.css :root block | Canonical color token definitions | Modified by agent; reverted via git if incorrect |
| User readability | WCAG AA compliance for all text surfaces | Contrast regression risk from incorrect token values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | app.css :root block token values | mitigate | Token verification via `color_token_inventory()` before Task 1 commit — zero Falha rows required. `contrast_ratio()` on all real pairs in Task 2 verify step (see Plan 02-03). |
| T-02-02 | Denial of Service (usability) | Visual readability of corrected colors | mitigate | Scripted `aa_status()` against all corrected values. DESIGN.md documents computed ratios. Git history enables rollback. |
| T-02-03 | Repudiation | Unauthorized token changes without audit trail | mitigate | Per-task atomic git commits. DESIGN.md is the documented source of truth. Phase 2 changes visible in git log. |

**ASVS L1 applicability:** Phase 2 is a CSS-only value change. No server-side code, no user input parsing, no authentication/session/crypto. ASVS L1 controls do not directly apply. The mitigations above address the relevant risks: contrast regression and auditability.
</threat_model>

<verification>
### Per-task verification
- Task 1: Run `color_token_inventory()` — assert zero Falha rows
- Task 2: Grep for `var(--negative-ink)` — assert exactly 2 occurrences; grep for hardcoded `color: #fff;` in component context — assert 0

### Phase-level verification (across all plans)
- `uv run pytest tests/test_phase02_tokens.py -v` — all tests pass (Plan 02-03)
- `uv run pytest tests/test_audit_css_parser.py tests/test_audit_color_resolver.py -v` — existing 38 tests still pass
- DESIGN.md token table values match app.css :root block values (Plan 02-02 cross-check)
</verification>

<success_criteria>
1. `color_token_inventory()` against corrected `app.css` returns zero Falha rows (was 2: class-4, class-6)
2. Corrected token pairs exceed WCAG AA thresholds: class-4 vs --bg ≥ 4.5 (actual 5.16), class-6 vs --bg ≥ 4.5 (actual 4.89), negative-ink on negative ≥ 4.5 (actual 6.59), error-fg on error-bg ≥ 4.5 (actual 7.79)
3. `--negative-ink` and `--positive-ink` tokens present in :root with value `#ffffff`
4. `--error-bg` and `--error-fg` use OKLCH format (not hex)
5. Both delete-confirm button rules reference `var(--negative-ink)` instead of hardcoded `#fff`
6. Legacy aliases (`--fg: var(--ink)`, `--muted: var(--ink-muted)`) preserved
</success_criteria>

<output>
Create `.planning/phases/02-palette/02-01-SUMMARY.md` when done
</output>
