---
phase: 02-palette
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DESIGN.md
autonomous: true
requirements:
  - PALT-03
  - PALT-02
must_haves:
  truths:
    - "DESIGN.md token table has Contrast column showing computed WCAG AA ratio and Passa/Falha status per D-02"
    - "DESIGN.md token table includes all 25 tokens (existing + new --negative-ink, --positive-ink + legacy aliases)"
    - "DESIGN.md class swatches table shows corrected slot 4 at oklch(0.53 0.13 50)"
    - "DESIGN.md Component inventory table has Color Tokens column per D-04"
    - "DESIGN.md Color strategy section references WCAG AA verification provenance"
    - "DESIGN.md accent rationale section is preserved unchanged"
    - "DESIGN.md contains no version history or changelog per D-03"
    - "DESIGN.md migration path step 1 references completed Phase 2 corrections"
  artifacts:
    - path: "DESIGN.md"
      provides: "Living design system documentation with corrected color tokens"
      contains: "Contrast"
      contains: "Passa"
      contains: "Color Tokens"
      contains: "oklch(0.53 0.13 50)"
      contains: "--negative-ink"
  key_links:
    - from: "DESIGN.md token table"
      to: "app.css :root block"
      via: "token names (e.g., --class-4) match between documents"
      pattern: "`--class-4`"
    - from: "DESIGN.md component inventory"
      to: "app.css component rules"
      via: "Color Tokens column references token names used in component CSS rules"
      pattern: "--accent, --accent-ink"
---

<objective>
Rewrite DESIGN.md color-related sections to reflect the corrected Phase 2 token system. Per D-01: full color section refresh including Color strategy, Accent rationale (preserved), Class swatches, Token table, and Migration path. Per D-02: add Contrast column to token table. Per D-04: annotate Component inventory with Color Tokens column. Per D-03: no version history or changelog.

Purpose: Satisfies PALT-03 (DESIGN.md reflects corrected token values and rationale). The DESIGN.md is the human-readable source of truth that documents what the token system is, why values were chosen, and how to verify them.

Output: Rewritten DESIGN.md with expanded 7-column token table (~25 rows), corrected class swatches table, annotated component inventory, and updated migration path.
</objective>

<execution_context>
@/home/italo/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/italo/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-palette/02-CONTEXT.md
@.planning/phases/02-palette/02-RESEARCH.md
@.planning/phases/02-palette/02-PATTERNS.md
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Color strategy, Token table, and Class swatches sections per D-01 and D-02</name>
  <files>DESIGN.md</files>
  <read_first>
DESIGN.md (lines 1–69 — Color strategy, Token table, Accent rationale, Class swatches)
.planning/phases/02-palette/02-RESEARCH.md (lines 398–427 — DESIGN.md Update Strategy for exact table structure)
.planning/phases/02-palette/02-RESEARCH.md (lines 282–315 — Corrected Token Values for all new/changed values)
.planning/phases/02-palette/02-PATTERNS.md (lines 17–74 — DESIGN.md section edit patterns)
  </read_first>
  <action>
Edit `DESIGN.md` to update three sections — do NOT replace the entire file. Use targeted edits:

**Section 1 — Color strategy (lines 14–23):**
Edit the paragraph to add WCAG AA verification provenance. Change the text to include:
```
The body surface is a true off-white, not a cream. ...
The accent is one color, used at ≤10% of the surface, committed.
All token values were verified against WCAG 2.1 AA via scripted
`contrast_ratio()` + `aa_status()` checks (coloraide 8.8.1, wcag21 method).
The Phase 1 audit identified 2 genuine contrast failures (`--class-4` at
2.87, `--class-6` at 4.21); both were corrected in Phase 2.
```

**Section 2 — Token table (lines 30–42):**
Replace the existing 11-row, 4-column table with an expanded table using the 7-column format from RESEARCH.md §DESIGN.md Update Strategy (lines 412–427):

Columns: `Token | OKLCH | Hex (approx) | Role | Pair | Contrast | Status`

Include ALL 25 tokens. Use these exact rows (in order):

```
| `--bg` | `oklch(0.975 0.003 60)` | `#fafaf7` | Body surface | vs `--ink` | 16.85 | Passa |
| `--surface` | `oklch(1.0 0 0)` | `#ffffff` | Card/modal surface | vs `--ink` | 18.12 | Passa |
| `--surface-sunk` | `oklch(0.96 0.003 60)` | `#f5f5f2` | Form wells, table headers | vs `--ink` | 16.13 | Passa |
| `--ink` | `oklch(0.20 0.01 60)` | `#1a1a1a` | Primary text | vs `--bg` | 16.85 | Passa |
| `--ink-muted` | `oklch(0.50 0.01 60)` | `#6b6b6b` | Secondary text, labels | vs `--bg` | 5.59 | Passa |
| `--fg` | `var(--ink)` | `#1a1a1a` | Legacy alias → `--ink` | vs `--bg` | 16.85 | Passa |
| `--muted` | `var(--ink-muted)` | `#6b6b6b` | Legacy alias → `--ink-muted` | vs `--bg` | 5.59 | Passa |
| `--border` | `oklch(0.90 0.005 60)` | `#d8d8d3` | Hairline borders | vs `--ink` | 13.44 | Passa (non-text) |
| `--border-strong` | `oklch(0.82 0.008 60)` | `#c4c4bf` | Card outer border | vs `--ink` | 10.37 | Passa (non-text) |
| `--accent` | `oklch(0.42 0.09 150)` | `#225a31` | Accent fill (fern green) | + `--accent-ink` | 7.67 | Passa |
| `--accent-ink` | `oklch(0.98 0.005 150)` | `#f0faf2` | Text on accent fill | + `--accent` | 7.67 | Passa |
| `--positive` | `oklch(0.52 0.13 145)` | `#2e7d32` | Gain/success text | vs `--bg` | 4.84 | Passa |
| `--positive-ink` | `#ffffff` | `#ffffff` | Text on positive fill | + `--positive` | — | Future (no current usage) |
| `--negative` | `oklch(0.50 0.18 25)` | `#b71c1c` | Loss/error text | vs `--bg` | 6.13 | Passa |
| `--negative-ink` | `#ffffff` | `#ffffff` | Text on negative fill | + `--negative` | 6.59 | Passa |
| `--error-bg` | `oklch(0.94 0.02 15)` | `#fde8e8` | Error block background | + `--error-fg` | 7.79 | Passa |
| `--error-fg` | `oklch(0.38 0.12 25)` | `#8a1f1f` | Error text | + `--error-bg` | 7.79 | Passa |
| `--class-1` | `#0a66c2` | `#0a66c2` | Blue swatch | vs `--bg` | 5.29 | Passa |
| `--class-2` | `#2e7d32` | `#2e7d32` | Green swatch | vs `--bg` | 4.77 | Passa |
| `--class-3` | `#c62828` | `#c62828` | Red swatch | vs `--bg` | 5.23 | Passa |
| `--class-4` | `oklch(0.53 0.13 50)` | `#c44900` | Orange swatch | vs `--bg` | 5.16 | Passa (was #ef6c00 at 2.87 Falha) |
| `--class-5` | `#6a1b9a` | `#6a1b9a` | Purple swatch | vs `--bg` | 8.73 | Passa |
| `--class-6` | `oklch(0.52 0.10 200)` | `#007a85` | Teal swatch | vs `--bg` | 4.89 | Passa (was #00838f at 4.21 Falha) |
| `--color-focus` | `#2563eb` | `#2563eb` | Focus ring | vs `--bg` | 4.81 | Passa |

Add a note immediately below the table:
```
**Contrast methodology:** Ratios computed via `coloraide.Color(fg).contrast(bg, method="wcag21")`.
Foreground tokens tested against `--bg` (body surface). Surface tokens tested against `--ink` (default text).
Paired tokens (e.g., `--accent-ink` + `--accent`) show the real usage pair, not the inventory heuristic.
Canonical values are OKLCH. Hex approximations are for visual reference only — do not use in CSS.
```

Remove the old "Tokens (target)" heading and rename to `### Token table (corrected)`.

**Section 3 — Class swatches table (lines 53–69):**
Replace the 6-row class swatches table with an expanded version including the corrected slot 4 and a Contrast column:

Columns: `Slot | OKLCH | Role | Contrast vs --bg | Status`

```
| Slot  | OKLCH                    | Role                                    | Contrast vs --bg | Status |
|-------|--------------------------|-----------------------------------------|------------------|--------|
| 1     | `oklch(0.50 0.14 250)`   | Deep blue (replaces `#0a66c2` brand-ish)| 5.29             | Passa  |
| 2     | `oklch(0.50 0.13 145)`   | Deep green (close to `--accent` but distinct) | 4.77       | Passa  |
| 3     | `oklch(0.50 0.18 25)`    | Deep red (matches `--negative`)         | 5.23             | Passa  |
| 4     | `oklch(0.53 0.13 50)`    | Burnt orange (not tangerine). Corrected from 0.62→0.53 for AA contrast. | 5.16 | Passa |
| 5     | `oklch(0.42 0.12 300)`   | Deep plum (not vibrant purple)          | 8.73             | Passa  |
| 6     | `oklch(0.52 0.10 200)`   | Deep teal (not cyan)                    | 4.89             | Passa  |
```

Update the paragraph below the table (lines 67–69, "The 7th+ class cycles...") to note:
```
The 7th+ class cycles via the existing `nth-of-type(6n+N)` rules in `app.css`.
The hex values previously in CSS (`#0a66c2`, `#2e7d32`, etc.) were the migration source;
`--class-4` and `--class-6` now use corrected OKLCH values that meet WCAG AA.
```

**Section 4 — Accent rationale (lines 44–51):**
Do NOT modify this section. Per CONTEXT.md §Specific Ideas: "DESIGN.md accent rationale (hue 150 fern green) should be preserved — it's a deliberate design choice, not a defect." Leave lines 44–51 exactly as they are. The token table (Section 2 above) already documents the accent's contrast status, which is the only Phase 2 concern for this section.

**Section 5 — DESIGN.md header (lines 1–7):**
Update line 5 (`"direction the polish pass is aiming for"`) to reflect Phase 2 completion:
```
Working visual system for Omaha. This is a **living** doc. The CSS in
`src/omaha/static/app.css` is the current state. Phase 2 (2026-06-13)
applied WCAG 2.1 AA-verified color token corrections; the values below
reflect the corrected token system.
```

**Verification checkpoint:**
- `grep -c 'Contrast' DESIGN.md` must show ≥ 3 (header in token table, class swatches, and one in prose)
- `grep -c 'Passa' DESIGN.md` must show ≥ 20 (most tokens pass)
- `grep -c 'Falha' DESIGN.md` must be ≤ 4 (only in "was ... Falha" historical notation for class-4 and class-6 in token table and class swatches)
- `grep -c 'oklch(0.53 0.13 50)' DESIGN.md` must be ≥ 1 (corrected class-4 appears in token table and class swatches)
- `grep -c '--negative-ink' DESIGN.md` must be ≥ 1 (new token appears in token table)
  </action>
  <verify>
    <automated>bash -c 'echo "Contrast column:"; grep -c "Contrast" DESIGN.md; echo "Passa count:"; grep -c "Passa" DESIGN.md; echo "oklch(0.53) in DESIGN.md:"; grep -c "oklch(0.53 0.13 50)" DESIGN.md; echo "negative-ink in DESIGN.md:"; grep -c "negative-ink" DESIGN.md'</automated>
  </verify>
  <done>
- Token table has 7 columns including Contrast and Status with all 25 tokens
- Class swatches table has corrected slot 4 at oklch(0.53 0.13 50) with 5.16 Passa contrast
- Color strategy section references WCAG AA verification provenance
- Accent rationale preserved unchanged (grep for "Accent rationale" still present before token table)
- No version history, no changelog added (per D-03)
- DESIGN.md header updated to reference Phase 2
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Migration path and annotate Component inventory per D-04</name>
  <files>DESIGN.md</files>
  <read_first>
DESIGN.md (lines 175–227 — Component inventory and Migration path sections)
.planning/phases/02-palette/02-RESEARCH.md (lines 429–446 — Component Inventory Annotations reference table)
.planning/phases/02-palette/02-PATTERNS.md (lines 45–61 — Component inventory edit pattern and D-04 annotation format)
  </read_first>
  <action>
**Step 1 — Component inventory (lines 175–192):**
Replace the existing 13-row, 3-column table with an expanded table adding a "Color Tokens" column per D-04. Use the exact annotations from RESEARCH.md §Component Inventory Annotations (lines 433–446):

New columns: `Component | Where | Color Tokens | Notes`

```
| Component           | Where                  | Color Tokens | Notes                                  |
|---------------------|------------------------|-------------|----------------------------------------|
| App header          | `base.html`            | `--surface`, `--border`, `--fg`, `--muted`, `--accent` | Flat, border-bottom. Logo, nav, profile, signout. |
| Profile picker      | `profiles.html`        | `--ink`, `--surface`, `--border` | Two large buttons. Quiet, no marketing. |
| Login               | `login.html`           | `--bg`, `--ink`, `--ink-muted`, `--accent`, `--accent-ink` | Single field, single button, error inline. |
| Portfolio header    | `dashboard.html`       | `--fg`, `--positive`, `--negative` | Invested / current / gain. Conditional on gain/loss. |
| Class section       | `dashboard.html`       | `--surface`, `--border`, `--ink`, `--ink-muted` | Swatch + name + compare bar + asset list. |
| Compare bar         | `dashboard.html`       | `--border-strong` (target), `--accent` (current fill) | Two stacked fills. |
| Asset row           | `dashboard.html`       | `--ink`, `--ink-muted`, `--positive`, `--negative` | Name + value + pct + progress bar. |
| Progress bar        | `dashboard.html`       | `--border` (track), `--accent` (fill) | Per-asset fill. |
| Class color swatch  | `dashboard.html`       | `--class-1..6` (cycled via nth-of-type) | 14px×14px, decorative. |
| Primary button      | various                | `--accent`, `--accent-ink` | hover: brightness(1.1). |
| Secondary button    | various                | `--surface-sunk`, `--ink`, `--border` | hover: `--border` bg. |
| Delete confirm      | `dashboard.html`       | `--negative`, `--negative-ink` | Confirm dialog. |
| Error message       | various                | `--error-bg`, `--error-fg` | Inline, top of form. No toast. |
| Empty state         | various                | `--ink-muted` | Single line, a link if actionable. |
| Focus ring          | all                    | `--color-focus` | 2px outline, 2px offset. |
| Form inputs         | various                | `--surface-sunk`, `--ink`, `--border`, `--accent` (focus border) | input, select, textarea. |
| Class table         | `classes.html`         | `--surface`, `--border`, `--ink`, `--ink-muted`, `--positive`, `--negative` | Editable rows, percent total at bottom. |
| Review table        | `import_review.html`   | `--surface`, `--border`, `--ink`, `--ink-muted` | Auto-matched summary + unmatched select. |
| Import form         | `import.html`          | `--bg`, `--ink`, `--ink-muted`, `--accent`, `--accent-ink` | File picker, single submit. |
| Asset editor        | `assets.html`          | `--surface`, `--border`, `--ink`, `--ink-muted`, `--accent`, `--negative` | Per-class sections, inline add/remove. |

**Step 2 — Migration path (lines 212–227):**
Update step 1 to reflect Phase 2 completion. Replace the current step 1 text:

Step 1 currently reads:
```
1. Update `:root` tokens in `app.css` to the OKLCH values above. Keep
   the class-color hex values inline (they are migration source) until
   the swatch palette is committed.
```

Replace with:
```
1. ~~Update `:root` tokens in `app.css` to the OKLCH values above.~~ ✓ Phase 2 complete.
   Token values corrected: `--class-4` (2.87→5.16), `--class-6` (4.21→4.89).
   Added: `--negative-ink`, `--positive-ink`. Converted: `--error-bg`, `--error-fg` to OKLCH.
   Delete-confirm buttons now use `var(--negative-ink)` instead of hardcoded `#fff`.
   All 25 tokens pass WCAG 2.1 AA (verified via `color_token_inventory()` + `aa_status()`).
```

Steps 2–6 remain unchanged (they describe future polish work — removing shadows, migration to off-white, tnum, animations, serif font — none in scope for Phase 2).

Do NOT add version history or changelog (per D-03). The ~~strikethrough~~ on step 1 is documentation, not a changelog.
  </action>
  <verify>
    <automated>bash -c 'echo "Color Tokens column:"; grep -c "Color Tokens" DESIGN.md; echo "negative-ink in inventory:"; grep -c "negative-ink" DESIGN.md; echo "accent-ink in inventory:"; grep -c "accent-ink" DESIGN.md; echo "Phase 2 complete:"; grep -c "Phase 2 complete" DESIGN.md'</automated>
  </verify>
  <done>
- Component inventory table has Color Tokens column (per D-04) — 20 rows annotated with token usage
- Delete confirm row shows `--negative`, `--negative-ink`
- Primary button row shows `--accent`, `--accent-ink`
- Migration path step 1 struck through with Phase 2 completion note
- Steps 2–6 of migration path unchanged
- No version history added to DESIGN.md (per D-03)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → DESIGN.md | Markdown file, no executable content; edited by agent, reviewed by human |
| DESIGN.md → Human reader | Documentation accuracy — incorrect contrast ratios mislead future decision-making |

## Assets at Risk

| Asset | Value | Exposure |
|-------|-------|----------|
| DESIGN.md token table | Canonical reference for all color decisions | Agent writes values; mismatch with app.css would cause future confusion |
| DESIGN.md contrast column | WCAG AA compliance documentation | Inaccurate ratios would provide false confidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | DESIGN.md token values | mitigate | Cross-check: grep token values in DESIGN.md against app.css :root block after Phase 2. Plan 02-03 verification includes DESIGN.md/app.css consistency check. |
| T-02-05 | Information Disclosure | None — DESIGN.md is internal documentation | accept | No sensitive data; single-household app. DESIGN.md is source-controlled reference. |
| T-02-06 | Repudiation | DESIGN.md content changes | mitigate | Per-task git commit. D-03 explicitly prohibits changelog; changes are tracked in git history and SUMMARY.md. |

**ASVS L1 applicability:** DESIGN.md is markdown documentation. No authentication, session, crypto, or user input involved. ASVS L1 controls do not directly apply. The mitigation above addresses the relevant risk: documentation accuracy.
</threat_model>

<verification>
### Per-task verification
- Task 1: Grep DESIGN.md for Contrast column, Passa count, corrected class-4 value, negative-ink token
- Task 2: Grep DESIGN.md for Color Tokens column header, token presence in inventory rows, Phase 2 migration note

### Cross-plan verification
- DESIGN.md token table values must match app.css :root block values (Plan 02-03 will verify)
- No version history/changelog added (D-03 compliance)
- Accent rationale unchanged (grep for original text)
</verification>

<success_criteria>
1. DESIGN.md token table expanded from 11-row × 4-col to ~25-row × 7-col with Contrast and Status columns (D-02)
2. Class swatches table shows corrected slot 4 at oklch(0.53 0.13 50) with 5.16 Passa
3. Color strategy section references WCAG AA verification via coloraide contrast_ratio() + aa_status()
4. Accent rationale section preserved unchanged (per CONTEXT.md)
5. Component inventory annotated with Color Tokens column showing token usage per component (D-04)
6. Migration path step 1 struck through with Phase 2 completion note
7. No version history or changelog section added (D-03)
8. Header updated to note Phase 2 completion date
</success_criteria>

<output>
Create `.planning/phases/02-palette/02-02-SUMMARY.md` when done
</output>
